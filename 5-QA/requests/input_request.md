# Input Request — QA Persona

**Requested By:** SteeringDirections
**Focus:** Pilot evidence + G4 gating

## Required Inputs
1. Populate `planning/50_pilot/evidence/qa/template_metrics.md` after each Basel-I run (flow success %, tokens, mutation kill-rate, BLOCKERS).
2. Attach supporting CI artifacts/logs in `planning/50_pilot/evidence/qa/` with filenames `run-YYYYMMDD.md` and zipped logs when necessary.
3. Update `5-QA/artifacts/quality_plan.md` with any changes to thresholds or gating order discovered during Build.

Notify SteeringDirections upon completion to include in G5 deck.
